import { DynamicComponentData } from './dynamic-component-data';

describe('DynamicComponentData', () => {
  it('should create an instance', () => {
    expect(new DynamicComponentData()).toBeTruthy();
  });
});
